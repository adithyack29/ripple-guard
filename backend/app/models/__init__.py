"""Internal domain models (e.g. graph node/edge representations).

These describe RippleGuard's internal understanding of the dependency
ecosystem and are independent of any single API request/response shape.
Contrast with `app.schemas`, which defines the API's request/response
contracts. No models are defined yet — this boundary is prepared for
Phase 1 (dependency ingestion) and Phase 2 (graph construction).
"""
