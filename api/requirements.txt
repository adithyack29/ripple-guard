# Runtime dependencies for api/index.py (the Vercel serverless function).
# Mirrors backend/requirements.txt minus dev/test-only and local-server-only
# packages (pytest, pytest-asyncio, uvicorn — Vercel invokes the ASGI app
# directly, it does not run a uvicorn server).
fastapi>=0.110,<1.0
python-multipart>=0.0.9,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
networkx>=3.2,<4.0
httpx>=0.27,<1.0
upstash-redis>=1.2,<2.0
